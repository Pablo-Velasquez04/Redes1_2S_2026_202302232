Mi carnet: 202302232

Fases completadas, pasos que se hicieron:
Fase1
¡Perfecto! Vamos a darle identidad a cada equipo y a configurar la seguridad básica obligatoria que solicita el documento.

Para hacer esto en Cisco Packet Tracer, deberás ingresar a la **Línea de Comandos (CLI)** de cada switch.

### Instrucciones paso a paso (Cómo hacerlo en Packet Tracer)

1. Haz clic izquierdo sobre un switch (empecemos por el principal, el que conecta a todos los edificios).
2. En la ventana que se abre, ve a la pestaña que dice **CLI** (Command Line Interface).
3. Si ves un mensaje que dice `Press RETURN to get started!`, presiona la tecla **Enter** en tu teclado. Verás el prompt del switch, que normalmente dice `Switch>`.
4. Escribe los siguientes comandos (presiona Enter después de cada línea):

```ios
enable
configure terminal

```

*(Ahora notarás que el prompt cambia a `Switch(config)#`, lo que significa que estás en el modo de configuración global).*

### 1. Asignar el Nombre y el Mensaje MOTD

A continuación, cambiaremos el nombre del equipo y configuraremos el banner exactamente como lo pide el proyecto. Ingresa estos comandos (copia el texto tal cual):

```ios
hostname Core_DataCenter
banner motd #Acceso Restringido - TechPark_202302232#
exit
write memory

```

*Nota: El símbolo `#` se usa como delimitador para indicarle al switch dónde empieza y dónde termina el mensaje del banner. El comando `write memory` (o simplemente `write`) sirve para guardar la configuración para que no se borre si apagas el simulador.*

---

### 2. Repetir en el resto de los switches

Debes repetir este mismo proceso en **todos** los switches de tu topología. Para mantener un orden impecable, te sugiero asignarles los siguientes nombres usando el comando `hostname`:

**Centro de Datos:**

* Switch principal: `hostname Core_DataCenter`
* Switch de los servidores: `hostname SW_Servidores`

**Centro de I+D:**

* Switch 1 (el que conecta al Core): `hostname ID_SW1`
* Switch 2: `hostname ID_SW2`
* Switch 3: `hostname ID_SW3`

**Edificio Corporativo:**

* Switch Ala A: `hostname Corp_AlaA`
* Switch Ala B: `hostname Corp_AlaB`

**Planta de Producción:**

* Switch Legacy: `hostname Prod_Legacy`

El comando del banner (`banner motd #Acceso Restringido - TechPark_202302232#`) será **exactamente el mismo** para todos ellos, ya que es un requerimiento de seguridad general.

1. **Comprobar que funcionó:** Verificación rápida.
Para asegurarte de que lo hiciste bien, presiona **Enter** un par de veces en la CLI de cualquier switch del que ya hayas salido (que esté en `Core_DataCenter>`). Deberías ver aparecer en la pantalla tu mensaje de acceso restringido justo antes de dejarte teclear comandos.



Fase2:
¡Perfecto! Vamos a darle identidad a cada equipo y a configurar la seguridad básica obligatoria que solicita el documento.

Para hacer esto en Cisco Packet Tracer, deberás ingresar a la **Línea de Comandos (CLI)** de cada switch.

### Instrucciones paso a paso (Cómo hacerlo en Packet Tracer)

1. Haz clic izquierdo sobre un switch (empecemos por el principal, el que conecta a todos los edificios).
2. En la ventana que se abre, ve a la pestaña que dice **CLI** (Command Line Interface).
3. Si ves un mensaje que dice `Press RETURN to get started!`, presiona la tecla **Enter** en tu teclado. Verás el prompt del switch, que normalmente dice `Switch>`.
4. Escribe los siguientes comandos (presiona Enter después de cada línea):

```ios
enable
configure terminal

```

*(Ahora notarás que el prompt cambia a `Switch(config)#`, lo que significa que estás en el modo de configuración global).*

### 1. Asignar el Nombre y el Mensaje MOTD

A continuación, cambiaremos el nombre del equipo y configuraremos el banner exactamente como lo pide el proyecto. Ingresa estos comandos (copia el texto tal cual):

```ios
hostname Core_DataCenter
banner motd #Acceso Restringido - TechPark_202302232#
exit
write memory

```

*Nota: El símbolo `#` se usa como delimitador para indicarle al switch dónde empieza y dónde termina el mensaje del banner. El comando `write memory` (o simplemente `write`) sirve para guardar la configuración para que no se borre si apagas el simulador.*

---

### 2. Repetir en el resto de los switches

Debes repetir este mismo proceso en **todos** los switches de tu topología. Para mantener un orden impecable, te sugiero asignarles los siguientes nombres usando el comando `hostname`:

**Centro de Datos:**

* Switch principal: `hostname Core_DataCenter`
* Switch de los servidores: `hostname SW_Servidores`

**Centro de I+D:**

* Switch 1 (el que conecta al Core): `hostname ID_SW1`
* Switch 2: `hostname ID_SW2`
* Switch 3: `hostname ID_SW3`

**Edificio Corporativo:**

* Switch Ala A: `hostname Corp_AlaA`
* Switch Ala B: `hostname Corp_AlaB`

**Planta de Producción:**

* Switch Legacy: `hostname Prod_Legacy`

El comando del banner (`banner motd #Acceso Restringido - TechPark_202302232#`) será **exactamente el mismo** para todos ellos, ya que es un requerimiento de seguridad general.

1. **Comprobar que funcionó:** Verificación rápida.
Para asegurarte de que lo hiciste bien, presiona **Enter** un par de veces en la CLI de cualquier switch del que ya hayas salido (que esté en `Core_DataCenter>`). Deberías ver aparecer en la pantalla tu mensaje de acceso restringido justo antes de dejarte teclear comandos.



Fase3:
Excelente. Entramos a una de las fases más críticas, donde conectaremos lógicamente los equipos.

Dado que tu carné es **202302232**, los cálculos para esta fase son:

* **VLAN Nativa:** Será la **VLAN 92** (porque tu último dígito es 2).


* **Dominio VTP:** Será **Smart_3** (porque tu penúltimo dígito es 3).



### Antes de empezar: Ver los números de los puertos

En Packet Tracer es fundamental saber qué puerto (ej. `Fa0/1` o `Gig0/1`) conecta a cada switch.

1. Ve al menú superior y selecciona **Options > Preferences**.
2. En la pestaña *Interface*, marca la casilla **"Always Show Port Labels in Logical Workspace"**. Esto hará que aparezcan las etiquetas de los puertos junto a los cables.

---

### Paso 1: Configurar el VTP y crear la VLAN Nativa (En el Switch Core)

Entra a la pestaña **CLI** de tu switch principal (`Core_DataCenter`) y aplica esta configuración para convertirlo en el servidor maestro y crear la VLAN 92 que usaremos como nativa:

```ios
enable
configure terminal
vtp mode server
vtp domain Smart_3
vtp password proyecto12S2026
vlan 92
name NATIVA
exit

```

*Nota: El switch Core distribuirá esta VLAN y este dominio al resto de la red.*

---

### Paso 2: Configurar VTP en el resto de los Switches (Clientes)

Ahora debes ir a la **CLI** de **todos los demás switches** de la topología (Servidores, I+D, Edificio Corporativo y Producción) e ingresar exactamente esto para que obedezcan al servidor:

```ios
enable
configure terminal
vtp mode client
vtp domain Smart_3
vtp password proyecto12S2026

```

---

### Paso 3: Configurar los Enlaces Troncales y aplicar la VLAN Nativa

Los puertos troncales son los que conectan un switch con otro switch. **Debes hacer esto en ambos extremos de cada cable que conecte switches**.

Para no hacerlo uno por uno, Packet Tracer permite usar el comando `interface range` para seleccionar varios puertos al mismo tiempo. Por ejemplo, si tu Switch Core conecta a los otros edificios usando los puertos `Gig0/1`, `Gig0/2` y `Fa0/24`, la configuración sería:

**En el Switch Core:**

```ios
interface range Gig0/1-2, Fa0/24 
switchport mode trunk
switchport trunk native vlan 92
exit
write memory

```

**En los Switches Clientes:**
Ve a los switches que están al otro lado de esos cables, identifica el puerto que sube hacia el Core o hacia otro switch (por ejemplo, `Fa0/1`), y configúralo igual:

```ios
interface Fa0/1
switchport mode trunk
switchport trunk native vlan 92
exit
write memory

```

### ¿Cómo saber si funcionó?

En el switch Core, escribe el comando `show vtp status`. Deberías ver que dice "VTP Operating Mode: Server" y "VTP Domain Name: Smart_3". Además, las luces naranjas de los cables volverán a ponerse verdes tras unos segundos.











------------------------------------------------------------------------------------------------------------------------------------------------
(LA MÁS NUEVA Y QUE YA SE REALIZO Y COMPROBO, VER ESTA NADA MÁS)
Fase 4:
1. **Paso 1: Crear las VLANs en el Switch Servidor:** Switch Core_DataCenter.
Entra a la CLI del switch **`Core_DataCenter`** (el único switch en modo VTP Server) e ingresa estos comandos exactos para registrar la base de datos de VLANs:

```ios
enable
configure terminal
vlan 12
name Gerencia
vlan 22
name Investigacion
vlan 32
name Produccion
vlan 42
name Servidores
vlan 52
name Visitantes
exit
write memory

```

* **Verificación de éxito:** Ejecuta `show vlan brief` en el `Core_DataCenter`. Deberás ver listadas las VLANs 12, 22, 32, 42 y 52 con sus respectivos nombres y estado *active*.




2. **Paso 2: Comprobar la propagación VTP:** Switches Clientes.
Ve a la CLI de cualquiera de los switches clientes (ej. `ID_SW1`, `Corp_AlaA` o `Prod_Legacy`) sin configurar nada de VLANs en ellos y ejecuta:

```ios
show vlan brief

```

* **Verificación de éxito:** Las VLANs 12, 22, 32, 42 y 52 deben aparecer creadas automáticamente en la tabla de todos los switches clientes.




3. **Paso 3: Asignar puertos de acceso en cada Switch:** Asignación por Edificio.
Ahora debemos colocar los puertos hacia los dispositivos finales en modo acceso (`switchport mode access`) y asignarlos a su VLAN correspondiente.

*(Ajusta las interfaces `fa0/x` según los puertos exactos donde conectaste tus cables)*.

**1. Centro de Datos (`SW_Servidores`) — VLAN 42:**

```ios
enable
configure terminal
interface range fa0/1-4
switchport mode access
switchport access vlan 42
exit
write memory

```

**2. Centro de I+D (`ID_SW1`, `ID_SW2`, `ID_SW3`) — VLAN 22:**
Ejecuta esto en cada uno de los 3 switches de I+D para los puertos donde estén conectadas las PCs:

```ios
enable
configure terminal
interface range fa0/1-3  ! Usa el rango de puertos donde estén tus PCs
switchport mode access
switchport access vlan 22
exit
write memory

```

**3. Edificio Corporativo (`Corp_AlaA` y `Corp_AlaB`) — VLAN 12 y 52:**

* En **`Corp_AlaA`** (donde están las PCs de Gerencia y el Access Point de Visitantes):

```ios
enable
configure terminal
interface range fa0/1-3   ! Puertos hacia PCs administrativas (Gerencia)
switchport mode access
switchport access vlan 12
exit
interface fa0/10          ! Puerto donde conectaste el Access Point (Visitantes)
switchport mode access
switchport access vlan 52
exit
write memory

```

* En **`Corp_AlaB`**:

```ios
enable
configure terminal
interface range fa0/1-3   ! Puertos hacia PCs administrativas
switchport mode access
switchport access vlan 12
exit
write memory

```

**4. Planta de Producción (`Prod_Legacy`) — VLAN 32:**
En el switch que se conecta hacia el Hub de maquinaria industrial:

```ios
enable
configure terminal
interface fa0/1           ! Puerto conectado al Hub
switchport mode access
switchport access vlan 32
exit
write memory

```

* **Verificación de éxito:** Vuelve a ejecutar `show vlan brief` en cualquiera de los switches. Verás que las interfaces correspondientes ya no están en la VLAN 1, sino asociadas al ID de su VLAN respectiva.
